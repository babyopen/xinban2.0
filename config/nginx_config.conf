# 小摇筛选项目Nginx配置
# 适用于宝塔面板

server {
    listen 80;
    server_name www.xboyi.cn xboyi.cn;
    access_log /www/wwwlogs/xboyi.cn.access.log;
    error_log /www/wwwlogs/xboyi.cn.error.log;
    
    # 重定向HTTP到HTTPS（如果已配置SSL）
    # return 301 https://$server_name$request_uri;
    
    # 静态文件处理
    location / {
        try_files $uri @flask_app;
    }
    
    # 前端静态文件
    location ~* \.(html|css|js|ico|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot)$ {
        root /www/wwwroot/xboyi.cn/frontend;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
    }
    
    # Flask应用反向代理
    location @flask_app {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
    }
    
    # API路径直接代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # 处理404错误
    error_page 404 /404.html;
    location = /404.html {
        root /www/wwwroot/xboyi.cn/frontend;
    }
    
    # 处理500错误
    error_page 500 502 503 504 /500.html;
    location = /500.html {
        root /www/wwwroot/xboyi.cn/frontend;
    }
}

# HTTPS配置（如果需要）
# server {
#     listen 443 ssl http2;
#     server_name www.xboyi.cn xboyi.cn;
#     access_log /www/wwwlogs/xboyi.cn.ssl.access.log;
#     error_log /www/wwwlogs/xboyi.cn.ssl.error.log;
#     
#     # SSL证书配置
#     ssl_certificate /www/server/panel/vhost/cert/xboyi.cn/fullchain.pem;
#     ssl_certificate_key /www/server/panel/vhost/cert/xboyi.cn/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     add_header Strict-Transport-Security "max-age=31536000" always;
#     
#     # 静态文件处理
#     location / {
#         try_files $uri @flask_app_ssl;
#     }
#     
#     # 前端静态文件
#     location ~* \.(html|css|js|ico|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot)$ {
#         root /www/wwwroot/xboyi.cn/frontend;
#         expires 30d;
#         add_header Cache-Control "public, max-age=2592000";
#     }
#     
#     # Flask应用反向代理
#     location @flask_app_ssl {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#     
#     # API路径直接代理
#     location /api/ {
#         proxy_pass http://127.0.0.1:8000/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
